High-Level Purpose

The code provides functionality for:

Creating tasks
Listing/filtering tasks
Updating status/priority/due dates
Deleting tasks
Managing tags
Generating statistics
Persisting data to a JSON file

It acts as the business logic layer between the app UI/CLI and storage.
